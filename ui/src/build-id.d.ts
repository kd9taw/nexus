// Injected by Vite (see vite.config.ts `define`) — the build stamp shown in Settings so the
// operator can confirm which build is running (the product version is always "0.2.0").
declare const __BUILD_ID__: string
