[package]
publish = false
name = "ft1"
version = "0.2.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Safe Rust wrapper over libft1 — the FT1 4-CPM turbo weak-signal modem"

[dependencies]
ft1-sys = { path = "../ft1-sys" }
